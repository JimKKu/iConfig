#!/bin/bash

 deploy logs -n cpufp-$1 -f $2 

# if [[ "$1" == "biz" ]]; then
#     deploy logs -n cpufp-$1 -f $2 
# elif [[ "$1" == "plat" ]]; then
#     deploy getres pods -n cpufp-plat -o wide
# else
#     echo "用法: $0 [-biz | -plat]"
#     exit 1
# fi

